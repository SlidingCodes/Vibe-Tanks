name: deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-pi
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: run deploy.sh on pi
        run: /home/blin2h/deploy/Vibe-Tanks/scripts/deploy.sh
